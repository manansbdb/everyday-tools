/**
 * Optional support / donations section.
 * Keep `enabled: false` by default. Do not invent wallet addresses.
 * Enable only after you intentionally set a real address you control.
 */
export const supportConfig = {
  enabled: false,
  bitcoinAddress: '',
} as const
