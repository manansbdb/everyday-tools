# Note

`package-lock.json` is generated locally by `npm install` and was omitted from the initial publish due to MCP push size limits (~160KB). Run `npm install` after clone to create a lockfile matching `package.json`. CI uses `npm ci` when the lockfile is present, otherwise `npm install`.
